<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ course.course.title }}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <div class="container">
            <h1>{{ course.course.title }}</h1>
            <p class="subtitle">{{ course.course.description }}</p>
        </div>
    </header>

    <main class="container">
        {{ content | safe }}
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025 {{ course.course.title }} - Version {{ course.course.version }}</p>
        </div>
    </footer>
</body>
</html>
